"use strict"; var connection = new signalR.HubConnectionBuilder().withUrl("/hubs/notification").build(); connection.start().then(function () { console.info("Connected") }).catch(function (err) { return console.error(err.toString()) }); connection.on("AddNewnotification", function (isAdded) { if (isAdded) { reloadEmpNotificationDetails() } }); connection.on("Acknowledgement", function (isACK) { if (isACK) { reloadAdminNotificationDetails() } })